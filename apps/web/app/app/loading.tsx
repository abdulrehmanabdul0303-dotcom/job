/**
 * App Routes Loading State
 * 
 * Shown while authenticated app pages are loading.
 * Provides immediate feedback to users.
 */

import { PageLoader } from '@/components/ui/loading'

export default function AppLoading() {
  return <PageLoader />
}
